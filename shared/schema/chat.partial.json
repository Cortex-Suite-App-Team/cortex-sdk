{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://cortex.local/sdk/schema/chat.partial.json",
  "title": "chat::partial Payload",
  "description": "Streaming chunk of an in-progress answer. Multiple partials share the same turn_id and are followed by a chat::answer with answer_kind=final.",
  "type": "object",
  "additionalProperties": false,
  "required": ["content", "role", "turn_id"],
  "properties": {
    "content": {
      "type": "string",
      "description": "Partial assistant content chunk"
    },
    "role": {
      "type": "string",
      "const": "assistant"
    },
    "turn_id": {
      "type": "string",
      "description": "Turn correlation id; matches the subsequent chat::answer"
    }
  }
}
